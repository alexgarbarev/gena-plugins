{% include 'header' %}

#import "{{ prefix }}{{ module_name }}RouterInput.h"

@protocol CCTransitionHandler;
@protocol CCModuleFactory;

@interface {{ prefix }}{{ module_name }}Router : NSObject <{{ prefix }}{{ module_name }}RouterInput>

@property (nonatomic, weak) id<CCTransitionHandler> transitionHandler;
@property (nonatomic, strong) id<CCModuleFactory> moduleFactory;

@end
